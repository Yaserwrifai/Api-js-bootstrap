'// 1- get data from URL(API)
const api = "https://www.breakingbadapi.com/api/characters/";
async function getData() {
    try {
        const response = await fetch(api);
        const data = await response.json();
        console.log(data)
        printData(data)
    } catch (e) {
        console.log("error : >>", e);

    }
    try {
        const response = await fetch(api);
        const data = await response.json();
        console.log(data);
        printData(data);
    } catch (e) {
        console.log("error : >>", e);
    }
}

// 2-print data in header and in content of Page
function printData(data) {
    const select = document.getElementById("select");
    data.forEach((element) => {
                let myOption = document.createElement("option")
                myOption.value = element.name
                myOption.innerHTML = element.name
                select.appendChild(myOption)
                    // show data into  cards
                const select = document.getElementById("select");
                data.forEach((element) => {
                    let myOption = document.createElement("option");
                    myOption.value = element.name;
                    myOption.innerHTML = element.name;
                    select.appendChild(myOption);
                    // show data into  cards

                    const mycard = document.getElementById("cardid");
                    const mycard = document.getElementById("cardid");

                    let img = document.createElement("img");
                    img.setAttribute("src", element.img);
                    img.setAttribute("alt", "Not found");
                    img.classList.add("card-img-top");
                    let img = document.createElement("img");
                    img.setAttribute("src", element.img);
                    img.setAttribute("alt", "Not found");
                    img.classList.add("card-img-top");

                    let cardBody = document.createElement("div");
                    cardBody.classList.add("card-body");
                    let cardBody = document.createElement("div");
                    cardBody.classList.add("card-body");

                    let h5 = document.createElement("h5");
                    cardBody.classList.add("card-title");
                    h5.innerHTML = element.name;
                    let h5 = document.createElement("h5");
                    cardBody.classList.add("card-title");
                    h5.innerHTML = element.name;

                    let p = document.createElement("p");
                    cardBody.classList.add("card-text");
                    p.innerHTML = element.birthday
                    let p = document.createElement("p");
                    cardBody.classList.add("card-text");
                    p.innerHTML = element.birthday;

                    //let myButton = document.createElement("a");
                    //let myButton = document.createElement("a");

                    mycard.appendChild(img);
                    cardBody.appendChild(h5);
                    cardBody.appendChild(p);
                    // cardBody.appendChild(myButton);

                    mycard.appendChild(img);
                    cardBody.appendChild(h5);
                    cardBody.appendChild(p);
                    // cardBody.appendChild(myButton);
                    mycard.appendChild(cardBody);
                });
            }

            mycard.appendChild(cardBody);
            const createEvent = () => {
                // locate the HTML elemet we want to add the event to
                // once you can console.log the name the carachter ...we start filtering
                // call your filter function
            };

            //!  2) create filter function
            // const filterCharacters = (charactersList, selectedCharacter) => {
            //   // recieve the list of characters.
            //     // filter the list and use the selected character as filter criteria

        }
        //

);
//     console.log('filteredCharacters :>> ', filteredCharacters);

//    //call the create table function with the result of the filter

}
//     }
getData();


//=====================================================================
/*
    select.addEventListener("change", (event) => {
@@ -80,4 +90,4 @@ async function getCh(name) {
    return data.name
}'